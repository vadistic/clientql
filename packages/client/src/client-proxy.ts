import ApolloClient from 'apollo-client'

export interface ClientProxyProps {
  client?: ApolloClient<any>
}
/**
 * Client proxy is path proxy with callback pointing to apollo
 */
export const clientProxy = (): any => {
  return null
}
